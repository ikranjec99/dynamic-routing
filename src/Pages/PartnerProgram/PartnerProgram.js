import { Button } from "rsuite"
import { Page } from "../../UI/Page/Page"

export const PartnerProgram = () => {
    return (
        <Page header={'Become our partner'}>
            <Button children={'Click here to become partner!'} />
        </Page>
    )
}