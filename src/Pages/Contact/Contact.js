import { Button } from "rsuite"
import { Page } from "../../UI/Page/Page"

export const Contact = () => {
    return (
        <Page header={'Contact us'}>
            <Button children={'Click here to contact us!'} />
        </Page>
    )
}